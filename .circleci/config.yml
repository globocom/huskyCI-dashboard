version: 2

jobs:
  build:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run:
          name: "Check security using Yarn Audit"
          command: |
            make check-sec